# ============================================================
# Smart E-Commerce System - Python Dependencies
# Install using: pip install -r requirements.txt
# ============================================================

# ==========================
# ACTIVE DEPENDENCIES
# (Currently used in the project)
# ==========================

# Web Framework
Flask==3.0.0
Werkzeug==3.0.1

# Database (PostgreSQL)
psycopg2-binary==2.9.10

# Password Hashing (used in models/user.py & models/admin.py)
bcrypt==4.1.2

# Production WSGI Server (used via CLI: gunicorn -w 4 app:app)
gunicorn==21.2.0

# ==========================
# FUTURE-USE DEPENDENCIES
# (Planned for upcoming features)
# ==========================

# Forms & CSRF Protection — for server-side form validation and security
Flask-WTF==1.2.1
WTForms==3.1.1

# Environment Variables — for loading .env files securely in deployment
python-dotenv==1.0.0

# Date & Time Utilities — for advanced date parsing in analytics
python-dateutil==2.8.2

# Image Processing — for resizing/compressing product image uploads
Pillow==11.1.0

# Debug Toolbar — for development-time debugging of SQL, requests, templates
flask-debugtoolbar==0.14.1

# Rate Limiting — for protecting login endpoints
Flask-Limiter==3.5.0
